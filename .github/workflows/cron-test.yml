name: Simple Cron Test (Every 2 Minutes)

on:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  cron-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print timestamp
        run: |
          echo "✅ Cron job is running"
          echo "Time (UTC): $(date -u)"
